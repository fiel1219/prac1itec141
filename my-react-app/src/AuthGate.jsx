import { useEffect, useState } from 'react'
import App from './App'
import { supabase } from './lib/supabase'
export default function AuthGate(){const[session,setSession]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(true),[email,setEmail]=useState(''),[password,setPassword]=useState('');useEffect(()=>{if(!supabase){setError('Supabase is not configured');setLoading(false);return}supabase.auth.getSession().then(({data})=>{if(data.session)check(data.session);else setLoading(false)});const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>s?check(s):setSession(null));return()=>l.subscription.unsubscribe()},[]);async function check(s){const{data,error:x}=await supabase.from('user_accounts').select('role,status').eq('email',s.user.email).maybeSingle();if(x)setError(x.message);else if(data?.status==='active')setSession(s);else{await supabase.auth.signOut();setError('Your account is pending approval or disabled.')}setLoading(false)}async function login(e){e.preventDefault();setError('');setLoading(true);const{data,error:x}=await supabase.auth.signInWithPassword({email,password});if(x){setError(x.message);setLoading(false)}else check(data.session)}if(loading)return <div className="login-screen"><div className="login-card">Connecting securely…</div></div>;if(session)return <App/>;return <div className="login-screen"><form className="login-card" onSubmit={login}><div className="login-logo">⌁</div><p className="eyebrow">INVENTORY SYSTEM</p><h2>Welcome back</h2><p>Sign in to continue.</p>{error&&<div className="login-error">{error}</div>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary login-button">Sign in to Stockly</button></form></div>}
